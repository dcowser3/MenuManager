# Review learning — source-bound synthetic preflight v2

## Decision and evidence

Coordinator release for worker task `01a07857-38f1-7aa1-ab5f-d95509e0c952`, existing worktree and Luna Extra High setting. This supersedes the unsuccessful blank-line repair proposal and the requirement to reuse the contaminated synthetic responses as a positive readiness gate. It does NOT change production review code, historical data/truth, quality thresholds, prior result statuses or paid authority.

Audit `/tmp/mm-review-learning-layout-contract-audit.json`, SHA256 `119fa125b01947c75c6c38dca8a14a79f9a8d84ac5330859768e832ead595120`, establishes two distinct paths: guarded general AI spelling corrections in identity-preserving rows, and the stricter registered canonical-near-miss occurrence protocol. Absence of a registered near-miss is not a prohibition on every ordinary AI spelling edit.

The existing eight simulated responses copy human ground truth into corrected blocks. They introduce substantive content, not merely line breaks; the candidate's conservative unequal-row fallback discards edits rather than adopting that content. Therefore a positive readiness expectation that this fixture retain every human-final correction conflates human content changes with model corrections. Preserve the original `failed_safety`, 16-vs-3 metrics and exact artifacts. Do not claim this regression was repaired or the historical canary passed.

## Bounded implementation

Own only reusable synthetic-fixture preparation/validation, preflight contract versioning, associated tests and documentation. Do not edit review-pipeline, attribution, spelling policy, prompts, historical comparator, paid scoring or safety thresholds to accommodate this fixture.

1. Add an explicit versioned fixture manifest distinguishing positive source-preserving protocol simulation from adversarial human-final/full-block input. Bind source/raw/prechecked identities, arm/runtime/protocol identity, exact response bytes, expected mutation spans, simulated status/finish reason and expectation provenance. Fixtures are trusted test inputs prepared outside the candidate; model fields cannot mint the manifest's authority.
2. Build positive corrected blocks from immutable source and explicit test mutations, never by copying an entire human-final menu. Use original correction/explanation records only as exposed development expectations, not runtime policy. Each mutation needs exact source before/after, unique original UTF-16 span and provenance; validate bounds, source equality, nonoverlap, revision identity, existing protected/read-only constraints and unchanged structural skeleton. Repeated words require an independently identified exact occurrence; do not guess a nearest occurrence from target text or drop ambiguous cases silently.
3. Keep general spelling and registered contextual-near-miss fixtures distinct. General spelling exercises the existing guarded general lane. Registered cases must use actual server-issued IDs and existing dispositions; keep current contextual holds and original v3 outcomes unchanged. Do not invent registration for the historical13 signals or claim that simulated confidence establishes semantic permission.
4. Emit a coverage manifest listing every original canary correction and its status in the new synthetic fixture: source-bound included, independently ambiguous/unresolved, protected/held or unsupported protocol. Missing/ambiguous expectations remain explicit incomplete coverage; fixture preparation fails the positive gate when required coverage cannot be established. Do not declare readiness by silently omitting difficult cases.
5. Preserve the exact old human-final responses as adversarial controls. Assert that they cannot authorize new content, layout adoption, cross-dish correction transfers or a contextual apply claim. Their historical comparator score is diagnostic only, not a positive expectation for content adoption. Assert permitted existing behavior and conservative fallback accurately; do not rewrite the old artifacts or report a formerly failed run as passing.
6. Keep positive response generation independent of candidate outputs and identical across baseline/candidate where their source/protocol contracts permit. Any arm-specific protocol envelope must be explicitly versioned and map to the same intended source edits. Detect unexpected prechecked-source differences and report them rather than manufacturing arm advantages. Freeze before execution.
7. New preflight v2 has two separately reported gates: source-bound positive protocol/delivery checks, and adversarial source-preservation checks. Existing actual-output scoring remains untouched and visible, with semantic holds and unsupported legacy telemetry distinct. Passing local synthetic gates means only engineering readiness for a separately reviewed paid canary, not proof of improved model quality, historical correction recovery or project completion.

## Tests and acceptance

- Named fail-before/pass-after tests for truth-copying accepted as a positive fixture; arbitrary substantive additions, deleted rows, overlapping/stale spans, protected mutations, missing provenance and incomplete membership must be rejected before execution.
- Positive tests preserve exact source layout with ordinary spelling edits and applicable scoped deterministic rules; duplicate tokens with exact evidence versus ambiguous duplicates; Unicode offsets; actual occurrence disposition handling without changing policy.
- Execute unchanged old contaminated responses as negative controls and retain original metrics/status in the evidence chain.
- Rebuild affected tooling/packages as needed, run focused adapter/evaluation harness and new fixture-builder tests in pinned credential-free network-none Docker. Keep accepted B8/source/B7 evidence; no broad re-review of unchanged app code.
- Prepare a NEW immutable candidate/tooling snapshot and preflight plan with explicit v2 contract/fixture hashes and unchanged original historical baseline, dataset/truth, case IDs, prompt/rules/vocabulary, provider/model and scoring thresholds. Execution authorization stays unset. Run actual four-arm source-bound synthetic preflight plus adversarial controls; record raw delivered output, case membership, provenance and all missing coverage. Do not label simulated request accounting as paid spending.
- Update docs, ledger, acceptance map and README for changed reusable preflight behavior. State explicitly that original eight response fixtures were ground-truth copies and are no longer a valid positive model-response simulation; preserve reproducibility of their failed run.

## Boundaries and handoff

No production review logic edits, paid/provider calls, new semantic resources, extra model calls, deployment/activation, external notifications, shared stack restart, dish-catalog checks or billing changes. Original paid Stage1 remains 0 calls/$0 against cumulative $10 including retries/ambiguous requests. Its original historical data/model/quality gates remain applicable to actual paid outputs after a fresh internal release; v2 synthetic fixture repair does not authorize dispatch.

Return a concise direct handoff with exact delta, fixture generation invariants/provenance, complete coverage including unresolved cases, named tests and raw evidence, snapshot/manifest/tooling/plan hashes, positive/adversarial outcomes and remaining limitations. If original expectations cannot be independently source-bound, return that finite mapping gap instead of guessing or substituting easier cases. Coordinator reviews before any further release.
