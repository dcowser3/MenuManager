'use strict';

// C2b exposes the packet-compatible broker name while keeping the bounded
// code-candidate implementation separate from the historical Stage-1 worker.
module.exports = require('./code-proposal-broker');
