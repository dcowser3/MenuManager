# Demo menus

Sample menus used for sales demos and prospect walkthroughs. They are not test
fixtures — nothing in the test suite or the eval harness reads this folder.
(Eval/training inputs live in `Training Menus/` and `samples/`.)

Kept in git so a demo can be run from a fresh clone. Add new demo menus here
rather than at the repo root.
