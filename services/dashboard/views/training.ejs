<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600;700&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --color-primary: #2c2c2c;
            --color-secondary: #8b7355;
            --color-accent: #6b5344;
            --color-text: #333333;
            --color-text-light: #666666;
            --color-text-muted: #999999;
            --color-background: #ffffff;
            --color-background-alt: #fafaf8;
            --color-border: #e8e4de;
            --color-border-light: #f0ece6;
            --color-success: #4a7c59;
            --color-warning: #b8860b;
            --color-error: #8b3a3a;
            --font-heading: 'Cormorant Garamond', Georgia, serif;
            --font-body: 'Montserrat', -apple-system, BlinkMacSystemFont, sans-serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-body);
            background: var(--color-background);
            color: var(--color-text);
            line-height: 1.7;
            font-weight: 400;
            font-size: 15px;
            letter-spacing: 0.02em;
            min-height: 100vh;
        }

        .header {
            background: var(--color-background);
            color: var(--color-primary);
            padding: 2.5rem 4vw;
            border-bottom: 1px solid var(--color-border);
        }

        .header-content {
            max-width: 1400px;
            margin: 0 auto;
        }

        .header h1 {
            font-family: var(--font-heading);
            font-size: 2.2rem;
            font-weight: 500;
            letter-spacing: 0.05em;
            color: var(--color-primary);
        }

        .header p {
            color: var(--color-text-light);
            margin-top: 0.25rem;
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.15em;
            font-weight: 400;
        }

        .nav-links {
            margin-top: 1.5rem;
        }

        .nav-links a {
            color: var(--color-secondary);
            text-decoration: none;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            font-weight: 500;
            transition: color 0.2s ease;
        }

        .nav-links a:hover {
            color: var(--color-accent);
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 3rem 4vw;
        }

        .section {
            background: var(--color-background);
            border: 1px solid var(--color-border);
            padding: 2.5rem;
            margin-bottom: 2rem;
        }

        .section h2 {
            font-family: var(--font-heading);
            font-size: 1.5rem;
            font-weight: 500;
            letter-spacing: 0.03em;
            color: var(--color-primary);
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid var(--color-border);
        }

        .upload-area {
            border: 1px dashed var(--color-border);
            padding: 2.5rem;
            text-align: center;
            margin-bottom: 2rem;
            background: var(--color-background-alt);
        }

        .upload-area p {
            color: var(--color-text-light);
            margin-bottom: 2rem;
            font-size: 0.9rem;
        }

        .upload-form {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
            max-width: 500px;
            margin: 0 auto;
        }

        .file-input-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
            text-align: left;
        }

        .file-input-group label {
            font-size: 0.7rem;
            font-weight: 500;
            color: var(--color-text-muted);
            text-transform: uppercase;
            letter-spacing: 0.15em;
        }

        input[type="text"],
        input[type="number"] {
            padding: 1rem;
            border: 1px solid var(--color-border);
            font-size: 0.9rem;
            font-family: var(--font-body);
            transition: border-color 0.2s ease;
            background: var(--color-background);
        }

        input[type="text"]:focus,
        input[type="number"]:focus {
            outline: none;
            border-color: var(--color-secondary);
        }

        input[type="file"] {
            padding: 1rem;
            border: 1px solid var(--color-border);
            cursor: pointer;
            background: var(--color-background);
            font-family: var(--font-body);
            font-size: 0.85rem;
        }

        .btn {
            display: inline-block;
            padding: 1rem 2rem;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            border: 2px solid;
            cursor: pointer;
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 0.2em;
            font-family: var(--font-body);
            text-align: center;
            background: var(--color-primary);
            color: var(--color-background);
            border-color: var(--color-primary);
        }

        .btn:hover {
            background: transparent;
            color: var(--color-primary);
        }

        .btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        .btn-secondary {
            background: transparent;
            color: var(--color-text);
            border-color: var(--color-border);
        }

        .btn-secondary:hover {
            border-color: var(--color-primary);
            color: var(--color-primary);
        }

        .sessions-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 2rem;
        }

        .session-card {
            background: var(--color-background-alt);
            border: 1px solid var(--color-border);
            padding: 2rem;
            transition: all 0.3s ease;
        }

        .session-card:hover {
            border-color: var(--color-secondary);
        }

        .session-card h3 {
            font-family: var(--font-heading);
            font-size: 1.25rem;
            font-weight: 500;
            color: var(--color-primary);
            margin-bottom: 1.5rem;
        }

        .session-stats {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .stat {
            background: var(--color-background);
            border: 1px solid var(--color-border);
            padding: 1rem;
            text-align: center;
        }

        .stat-value {
            font-family: var(--font-heading);
            font-size: 2rem;
            font-weight: 500;
            color: var(--color-secondary);
        }

        .stat-label {
            font-size: 0.65rem;
            color: var(--color-text-muted);
            margin-top: 0.25rem;
            text-transform: uppercase;
            letter-spacing: 0.15em;
        }

        .session-actions {
            display: flex;
            gap: 1rem;
            margin-top: 1.5rem;
        }

        .session-actions button {
            flex: 1;
            padding: 0.75rem 1rem;
            font-size: 0.65rem;
        }

        .alert {
            padding: 1.25rem 1.5rem;
            margin-bottom: 2rem;
            border: 1px solid;
            font-size: 0.9rem;
        }

        .alert-success {
            background: rgba(74, 124, 89, 0.08);
            color: var(--color-success);
            border-color: rgba(74, 124, 89, 0.2);
        }

        .alert-error {
            background: rgba(139, 58, 58, 0.08);
            color: var(--color-error);
            border-color: rgba(139, 58, 58, 0.2);
        }

        .alert-info {
            background: rgba(139, 115, 85, 0.08);
            color: var(--color-secondary);
            border-color: rgba(139, 115, 85, 0.2);
        }

        .empty-state {
            text-align: center;
            padding: 5rem 2rem;
            color: var(--color-text-muted);
            border: 1px solid var(--color-border);
        }

        .empty-state-icon {
            font-size: 3rem;
            margin-bottom: 1.5rem;
            opacity: 0.3;
        }

        .empty-state h3 {
            font-family: var(--font-heading);
            font-size: 1.5rem;
            font-weight: 500;
            color: var(--color-text-light);
            margin-bottom: 0.5rem;
        }

        .empty-state p {
            font-size: 0.9rem;
        }

        .loading {
            display: none;
            text-align: center;
            padding: 2rem;
        }

        .loading.active {
            display: block;
        }

        .spinner {
            border: 2px solid var(--color-border);
            border-top: 2px solid var(--color-secondary);
            border-radius: 50%;
            width: 32px;
            height: 32px;
            animation: spin 1s linear infinite;
            margin: 0 auto;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .footer {
            text-align: center;
            padding: 3rem 4vw;
            color: var(--color-text-muted);
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 0.15em;
            border-top: 1px solid var(--color-border);
            margin-top: 2rem;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="header-content">
            <h1>Richard Sandoval Hospitality</h1>
            <p>Training Dashboard</p>
            <div class="nav-links">
                <a href="/">Back to Reviews</a>
            </div>
        </div>
    </div>

    <div class="container">
        <div id="alertContainer"></div>

        <!-- Upload Training Pair -->
        <div class="section">
            <h2>Upload Training Pair</h2>
            <div class="upload-area">
                <p>
                    Upload a pair of documents: original menu and human-redlined version
                </p>
                <form id="uploadPairForm" class="upload-form">
                    <div class="file-input-group">
                        <label>Pair Name (optional)</label>
                        <input
                            type="text"
                            name="pairName"
                            placeholder="e.g., casa_chi_nov_2024"
                        />
                    </div>

                    <div class="file-input-group">
                        <label>Original Document</label>
                        <input
                            type="file"
                            name="original"
                            accept=".docx"
                            required
                        />
                    </div>

                    <div class="file-input-group">
                        <label>Human-Redlined Version</label>
                        <input
                            type="file"
                            name="redlined"
                            accept=".docx"
                            required
                        />
                    </div>

                    <button type="submit" class="btn">Upload Pair</button>
                </form>
            </div>
        </div>

        <!-- Training Note -->
        <div class="section">
            <h2>Training</h2>
            <div class="alert alert-info">
                <strong>Note:</strong> The automated training pipeline has been deprecated.
                Training data is collected from uploaded pairs for future use.
            </div>
        </div>

        <!-- Training Sessions -->
        <div class="section">
            <h2>Training Sessions</h2>

            <% if (sessions.length === 0) { %>
                <div class="empty-state">
                    <div class="empty-state-icon">&#128218;</div>
                    <h3>No Training Sessions Yet</h3>
                    <p>Upload document pairs and run training to see results here</p>
                </div>
            <% } else { %>
                <div class="sessions-grid">
                    <% sessions.forEach(session => { %>
                        <div class="session-card">
                            <h3>Session <%= session.session_id %></h3>

                            <div class="session-stats">
                                <div class="stat">
                                    <div class="stat-value"><%= session.pairs_processed %></div>
                                    <div class="stat-label">Pairs</div>
                                </div>
                                <div class="stat">
                                    <div class="stat-value"><%= session.corrections_found %></div>
                                    <div class="stat-label">Corrections</div>
                                </div>
                                <div class="stat">
                                    <div class="stat-value"><%= session.rules_generated %></div>
                                    <div class="stat-label">Rules</div>
                                </div>
                                <div class="stat">
                                    <div class="stat-value">
                                        <%= session.corrections_found > 0 ?
                                            Math.round((session.rules_generated / session.corrections_found) * 100) : 0 %>%
                                    </div>
                                    <div class="stat-label">Efficiency</div>
                                </div>
                            </div>

                            <div class="session-actions">
                                <button
                                    class="btn btn-secondary"
                                    onclick="downloadRules('<%= session.session_id %>')"
                                >
                                    Download Rules
                                </button>
                                <button
                                    class="btn btn-secondary"
                                    onclick="downloadPrompt('<%= session.session_id %>')"
                                >
                                    Download Prompt
                                </button>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } %>
        </div>
    </div>

    <div class="footer">
        <p>&copy; <%= new Date().getFullYear() %> Richard Sandoval Hospitality</p>
    </div>

    <script>
        function showAlert(message, type = 'info') {
            const alertContainer = document.getElementById('alertContainer');
            const alertDiv = document.createElement('div');
            alertDiv.className = `alert alert-${type}`;
            alertDiv.innerHTML = `${message}`;
            alertContainer.appendChild(alertDiv);

            setTimeout(() => {
                alertDiv.remove();
            }, 5000);
        }

        document.getElementById('uploadPairForm').addEventListener('submit', async (e) => {
            e.preventDefault();

            const formData = new FormData(e.target);

            try {
                const response = await fetch('/training/upload-pair', {
                    method: 'POST',
                    body: formData
                });

                const result = await response.json();

                if (result.success) {
                    showAlert(`Training pair "${result.pairName}" uploaded successfully!`, 'success');
                    e.target.reset();
                } else {
                    showAlert(result.error || 'Upload failed', 'error');
                }
            } catch (error) {
                showAlert('Error uploading pair: ' + error.message, 'error');
            }
        });

        function downloadRules(sessionId) {
            window.location.href = `/training/download-rules/${sessionId}`;
        }

        function downloadPrompt(sessionId) {
            window.location.href = `/training/download-prompt/${sessionId}`;
        }
    </script>
</body>
</html>
