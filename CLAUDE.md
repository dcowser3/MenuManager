# CLAUDE.md

This project uses [AGENTS.md](AGENTS.md) as the canonical agent-instruction file. Read it first.

AGENTS.md keeps the always-applicable conventions inline and points to topic-specific docs (local dev, architecture, design decisions, etc.) — load those only when the task touches that area.
