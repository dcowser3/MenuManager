# Keep the reverse proxy above the dashboard's 15 MB per-file Multer limit.
# Multipart framing adds a small amount of request overhead, so nginx needs
# headroom beyond the application limit.
client_max_body_size 20m;
